'use client';
import { Button } from "@/components/ui/button";

export default function ScoutHeader() {
    return (
        <header className="bg-blue-500 text-white w-full h-16 flex items-center justify-center">
            <h1 className="text-xl font-bold">Temp</h1>
        </header>
    );
}
